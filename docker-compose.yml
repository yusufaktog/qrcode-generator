services:
  qr-generator:
    image: qr-generator
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - QR_CODE_WIDTH=250
      - QR_CODE_HEIGHT=250